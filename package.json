{
  "name": "@powerhousedao/recipes",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "test": "pnpm -r --filter='./audit-trail' --filter='./custom-read-model' --filter='./discord-webhook-processor' --filter='./full-text-search' --filter='./rate-limiter' --filter='./relational-db-subgraph' --filter='./signed-operations-verifier' --filter='./sync-health-monitor' test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
