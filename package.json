{
  "name": "@powerhousedao/recipes",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "start": "pnpm -r --filter '!@powerhousedao/subscription-cli' --filter '!@powerhousedao/sync-health-monitor' run start"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
