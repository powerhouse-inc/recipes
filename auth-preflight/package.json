{
  "name": "@powerhousedao/example-auth-preflight",
  "version": "0.0.0",
  "description": "Example: asking the reactor what it would decide before submitting — one preflight answers a batch of candidate operations, the verdict matches what the submit does, and it is a prediction rather than a promise",
  "license": "AGPL-3.0-only",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "start": "node --import tsx src/demo.ts",
    "generate": "ph-cli generate document-model --document document-models/expense-report/expense-report.json"
  },
  "dependencies": {
    "@powerhousedao/reactor": "catalog:",
    "@powerhousedao/reactor-browser": "catalog:",
    "@powerhousedao/shared": "catalog:",
    "document-model": "catalog:",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@powerhousedao/ph-cli": "catalog:",
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "vitest": "^4.1.0"
  }
}
