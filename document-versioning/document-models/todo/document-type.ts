/**
 * Shared by every version of the model: the document type identifies the
 * model, the version identifies the schema revision within it.
 */
export const todoDocumentType = "powerhouse/example-todo";
